---
apiVersion: v1
kind: Service
metadata:
  name: rule-service-group
  labels:
    app: rule
spec:
  ports:
  - port: 8070
    targetPort: 8070
    name: server
  selector:
    app: search
    ns: group
  type: ClusterIP