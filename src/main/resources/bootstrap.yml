spring:
  application:
    name: demo
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sekhrivijay/search-rule-service.git
          force-pull: true
          search-paths: /src/main/resources